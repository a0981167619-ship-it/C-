#include <iostream>
using namespace std;
int main()
{
	int answer=37,n;
	
	while(n!=answer)
	{
		cout<<"請輸入猜測的數字:";
		cin>>n;
		
		if(n>answer)
		   cout<<"猜的數字太大"<<'\n';
		else if(n<answer)
		   cout<<"猜的數字太小"<<endl;
		else
		    cout<<"猜對了!";
	}
	return 0;
}
